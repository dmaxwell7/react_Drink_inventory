import React from 'react';
import { Navbar } from '../Navbar';

export const Contact = () => {
  return (
    <div>
        <Navbar />
        Call me if your bored and would like to go out and have a Drink! must be 21 years or older.
    </div>
  )
}